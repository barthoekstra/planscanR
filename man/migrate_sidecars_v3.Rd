% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_sidecar.R
\name{migrate_sidecars_v3}
\alias{migrate_sidecars_v3}
\title{Upgrade every sidecar in a cache to the current schema (v3) in place.}
\usage{
migrate_sidecars_v3(cache_dir = NULL)
}
\arguments{
\item{cache_dir}{Optional cache root. Defaults to \code{\link[=cache_dir_default]{cache_dir_default()}}.}
}
\value{
The number of sidecars rewritten, invisibly.
}
\description{
Walks \verb{<cache>/files/.../<id>.meta.json}, reads each record, and rewrites it
through \code{\link[=write_record_sidecar]{write_record_sidecar()}} so on-disk paths become cache-relative,
\code{schema_version} is bumped to 3, and the v2 \verb{relevance_score_*} \code{extras}
duplication is dropped. The merging writer already upgrades a sidecar lazily
on the next write, so this is only needed to upgrade a whole cache eagerly in
one pass. Idempotent — re-running on a v3 cache just rewrites it unchanged.
}
\examples{
\dontrun{
migrate_sidecars_v3()
}
}
