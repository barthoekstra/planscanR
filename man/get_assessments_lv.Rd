% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_assessments_lv.R
\name{get_assessments_lv}
\alias{get_assessments_lv}
\title{Fetch environmental-assessment records from Latvia.}
\usage{
get_assessments_lv(
  date_range = NULL,
  limit = Inf,
  download = FALSE,
  cache_dir = NULL,
  overwrite = FALSE,
  max_file_size_mb = NULL,
  write_sidecar = TRUE,
  refresh = FALSE,
  topic = NULL,
  relevance_threshold = NULL,
  relevance_model = NULL,
  query = NULL,
  assessment_type = "All",
  ...
)
}
\arguments{
\item{date_range, limit, download, cache_dir, overwrite, max_file_size_mb, write_sidecar, refresh, topic, relevance_threshold, relevance_model}{See \code{\link[=get_assessments]{get_assessments()}}.}

\item{query}{Optional free-text query, matched client-side as a
case-insensitive substring of the record title.}

\item{assessment_type}{One of \code{"All"} (default), \code{"EIA"}, or \code{"SEA"}.
Selects which half (Views listing / SEA sub-pages) to crawl.}

\item{...}{Reserved for future extensions; unused arguments are warned
about.}
}
\value{
A tibble; see \code{\link[=get_assessments]{get_assessments()}} for the required schema.
}
\description{
Implementation of \code{\link[=get_assessments]{get_assessments()}} for Latvia. Backed by the
Environmental State Bureau (\emph{Vides pārraudzības valsts birojs}) portal at
\url{https://www.eva.gov.lv/}, a server-rendered Drupal site. The portal
exposes two \strong{structurally different} halves, merged into a single result
tibble and selected via an \code{assessment_type} argument:
}
\details{
\itemize{
\item \strong{EIA} — \emph{Ietekmes uz vidi novērtējums} (project-level EIA). A Drupal
Views listing at
\verb{https://www.eva.gov.lv/lv/ietekmes-uz-vidi-novertejumu-projekti?page=N}
whose rows each link to a per-project detail page.
\item \strong{SEA} — \emph{Stratēģiskais ietekmes uz vidi novērtējums} (plan/programme
SEA). Three flat sub-pages — \verb{/lv/atzinumi} (opinions), \verb{/lv/lemumi}
(decisions), \verb{/lv/monitorings} (monitoring) — each listing documents as
direct \verb{/lv/media/\{id\}/download?attachment} PDF links.
}

An \code{assessment_type} column (\code{"EIA"} / \code{"SEA"}) tags each row and is
preserved in the offline metadata cache so downstream tooling can tell them
apart without re-fetching anything; a \code{register} column carries the raw
sub-register label (\code{"ivn-projekti"} for EIA; \code{"atzinumi"} / \code{"lemumi"} /
\code{"monitorings"} for SEA). \code{document_id} is prefixed per register
(\code{"IVN-"}, \code{"ATZ-"}, \code{"LEM-"}, \code{"MON-"}) so the registers never collide on
disk.
}
\section{URL enumeration}{

The portal is server-rendered HTML (no JSON API). The two halves are
enumerated differently:
\itemize{
\item \strong{EIA} — the Views listing paginates via a \strong{0-indexed} \code{?page=N} query
parameter (~20 records per page). The portal's exposed-form filters are
POST/AJAX (a GET \verb{?combine=} is ignored), so this handler does a full
crawl \verb{?page=0,1,2,…}, stopping when a page yields no records. Each row
links to a per-project detail page (sidecar-first).
\item \strong{SEA} — each of the three flat sub-pages is fetched once. They are
year-grouped HTML tables (no pagination, no detail pages) whose rows carry
a direct \verb{/lv/media/\{id\}/download?attachment} PDF link, a document number,
a date, and the planning-document title.
}
}

\section{Geometry}{

None. The portal exposes location only as Latvian prose (cadastral numbers,
parishes, municipalities), surfaced for EIA records in the \code{location} extra
column. No coordinate geometry is available, so \code{geometry_path} /
\code{geometry_crs} are never set.
}

\section{Attachments}{

The two halves differ fundamentally:
\itemize{
\item \strong{EIA — metadata-only.} EIA detail pages carry metadata but \strong{no
downloadable document attachments} (decisions are referenced as prose /
numbers). Every EIA record therefore has \code{attachment_urls = character(0)};
documents are filled in downstream by \code{\link[=discover_attachments]{discover_attachments()}}
(\code{discover = TRUE}).
\item \strong{SEA — direct PDFs.} Each SEA sub-page row links to a public PDF at
\verb{https://www.eva.gov.lv/lv/media/\{id\}/download?attachment} (no
authentication; the server returns \code{application/pdf}). One attachment per
record.
}

Because the EIA half is metadata-only, the country's
\code{get_assessments_coverage()$status} starts with \verb{"supported (metadata-only},
which is the marker \code{discover_attachments()} keys on.
}

\section{Filter coverage (v0.1)}{

\itemize{
\item \code{assessment_type} — selects which half to crawl: \code{"All"} (default),
\code{"EIA"} (the Views listing), or \code{"SEA"} (the three sub-pages). Applied
here in R; the portal's own filters are POST/AJAX and not honoured.
\item \code{query} — matched client-side as a case-insensitive substring of the
record title.
\item \code{date_range} — matched client-side against \code{date_published} /
\code{date_decision}. A record whose date is \code{NA} is dropped only when a
\code{date_range} is explicitly set (matching the other handlers); SEA
monitoring entries that carry no date are kept when no \code{date_range} is
given.
\item \code{limit} — caps the total number of records returned across all crawled
registers.
}
}

\section{Performance}{

The EIA register is a few hundred projects across ~20-record pages; the
three SEA sub-pages are single fetches each. To be polite to the shared
government portal, LV requests are throttled to 5 requests per second by
default; override via \code{getOption("planscanR.lv_throttle_rate")}
(requests/sec; falsy disables). A \code{limit} keeps cold runs bounded; repeat
runs are sidecar-first.
}

\examples{
\dontrun{
# Quick smoke test
get_assessments_lv(limit = 3, download = FALSE)

# SEA only (the three flat sub-pages, with direct PDFs)
get_assessments_lv(assessment_type = "SEA", limit = 10, download = TRUE)

# Title substring
get_assessments_lv(query = "veja", limit = 20, download = FALSE)
}
}
\seealso{
\code{\link[=get_assessments]{get_assessments()}}, \code{\link[=get_assessments_coverage]{get_assessments_coverage()}}.
}
