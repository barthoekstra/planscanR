% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_public.R
\name{download_to_cache}
\alias{download_to_cache}
\title{Download one attachment URL into the planscanR cache.}
\usage{
download_to_cache(
  url,
  country,
  document_id,
  cache_dir = cache_dir_default(),
  max_file_size_mb = getOption("planscanR.max_file_size_mb", 50),
  overwrite = FALSE
)
}
\arguments{
\item{url}{Source URL (character scalar).}

\item{country}{ISO-2 country code.}

\item{document_id}{Portal-native document ID.}

\item{cache_dir}{Cache root. Defaults to \code{\link[=cache_dir_default]{cache_dir_default()}}.}

\item{max_file_size_mb}{Per-file size ceiling in MiB. Defaults to
\code{getOption("planscanR.max_file_size_mb", 50)}. \code{Inf}, \code{NULL}, or a
non-positive value means no cap.}

\item{overwrite}{If \code{FALSE} (default) and a non-empty file is already
cached, it is returned as-is with status \code{"exists"}. If \code{TRUE}, any
pre-existing copy (placeholder or finalized) is swept and the URL is
re-fetched.}
}
\value{
A one-row tibble with columns:
\code{url}, \code{local_path} (\code{NA} when skipped/failed), \code{size_bytes}, \code{sha256}
(\code{NA} when no local file), \code{status}, \code{reason}. \code{status} is one of
\code{"downloaded"}, \code{"exists"}, \code{"skipped_size"}, or \code{"failed"}.
}
\description{
Fetches a single URL into the planscanR cache, behaving exactly as
planscanR's own downloader does. In order, it:
}
\details{
\itemize{
\item resolves the destination path with \code{\link[=cache_path]{cache_path()}};
\item runs a cheap HEAD probe and skips the URL if its advertised
\code{Content-Length} already exceeds the cap, so an over-size file is never
transferred;
\item downloads through the shared throttled, retrying httr2 client;
\item re-checks the size cap after downloading and discards an over-cap file;
\item finalizes the file extension (a URL with no extension lands at a \code{.x}
placeholder, renamed from the response \code{Content-Type} or magic bytes);
\item computes the SHA-256 of the result.
}

This is the public, single-URL counterpart to planscanR's internal batch
downloader; it is intended for downstream callers (e.g. mirroring to
external storage) that need one file in the canonical cache location with a
structured result.
}
\examples{
\dontrun{
download_to_cache(
  "https://pas.commissiemer.nl/files/nl/3619/a3619ts.pdf",
  country = "nl",
  document_id = "3619"
)
}
}
\seealso{
\code{\link[=cache_path]{cache_path()}}, \code{\link[=resolve_cached_path]{resolve_cached_path()}}
}
