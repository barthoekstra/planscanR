% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_download.R
\name{clear_cache}
\alias{clear_cache}
\title{Invalidate (delete) part or all of the planscanR cache.}
\usage{
clear_cache(cache_dir = NULL, country = NULL, confirm = TRUE)
}
\arguments{
\item{cache_dir}{Optional cache root. Defaults to the
\code{getOption("planscanR.cache_dir")} value (which itself falls back to
\code{tools::R_user_dir("planscanR", "cache")}).}

\item{country}{Optional ISO-2 country code. If supplied, only that
country's subtree (\verb{<root>/files/<country>/}) is removed. Otherwise the
whole \verb{<root>/files/} tree is removed.}

\item{confirm}{If \code{TRUE} (default) and the session is interactive, print
a summary (path, file count, size) and ask for explicit y/n before
deleting. Set to \code{FALSE} for scripted/automated use.}
}
\value{
Invisibly, a tibble describing what was removed
(\code{path}, \code{n_files}, \code{bytes}, \code{removed}).
}
\description{
Use this when you actually want to force a refresh — for example after a
portal's HTML layout changes, or to free disk space. By default the
function asks for interactive confirmation before deleting anything, and
refuses to operate on directories outside the resolved cache root.
}
\details{
The cache is a single tree under \verb{<root>/files/<country>/<doc_id>/}
containing per-record sidecar JSON files plus any downloaded attachments.
\code{clear_cache()} removes that tree (or a country-scoped subset).
}
\examples{
\dontrun{
# Wipe everything under the default cache root, with confirmation prompt
clear_cache()

# Wipe only NL files (sidecars + attachments)
clear_cache(country = "nl")

# Scripted use (no prompt)
clear_cache(confirm = FALSE)
}
}
