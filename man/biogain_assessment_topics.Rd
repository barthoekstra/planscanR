% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topics.R
\name{biogain_assessment_topics}
\alias{biogain_assessment_topics}
\title{BIOGAIN-default topic phrases for relevance scoring.}
\usage{
biogain_assessment_topics()
}
\value{
A named character vector. Names are stable slugs used as column
suffixes; values are the topic phrases passed to the embedding model.
}
\description{
Returns the canonical set of search topics used by the BIOGAIN project
(Net Biodiversity Gain in spatial energy planning) when scoring
environmental-assessment records. The names of the returned vector are
the column-suffix slugs that will appear in the result tibble (e.g.
\code{relevance_score_wind}); the values are the English phrases embedded by
the multilingual model.
}
\details{
These topics are an \strong{opt-in default} — pass the return value as \code{topic}
to \code{\link[=get_assessments]{get_assessments()}} (or \code{score_assessments()}) when you want the
BIOGAIN set. Other use-cases should pass their own topic vector.

Cross-country notes:
\itemize{
\item \code{energy_transition_strategy} is intended to bridge NL \verb{Regionale Energie Strategie} (RES), DE \code{Klimaschutzkonzept} / regional energy plans,
FR \code{SRADDET} / \code{PCAET}, AT \code{Energiestrategie}, and similar instruments.
\item \code{renewable_zoning} is intended to bridge spatial-designation instruments
like NL \code{zoekgebieden}, DE \code{Vorrangzonen}, and EU RED III "renewable
acceleration areas".
\item The English phrases are deliberately generic so the multilingual
embedding model can semantically bridge to each portal's vocabulary
without translation.
}
}
\examples{
biogain_assessment_topics()

\dontrun{
# Score the whole NL register against the BIOGAIN topic set
res <- get_assessments(
  "nl",
  topic = biogain_assessment_topics(),
  download = FALSE
)
}
}
