% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_public.R
\name{resolve_cached_path}
\alias{resolve_cached_path}
\title{Locate the on-disk file for a (possibly placeholder) cache path.}
\usage{
resolve_cached_path(dest)
}
\arguments{
\item{dest}{A path as returned by \code{\link[=cache_path]{cache_path()}}.}
}
\value{
The resolved absolute path, or \code{NA_character_}.
}
\description{
\code{\link[=cache_path]{cache_path()}} returns a \code{.x} placeholder path for URLs whose path carries
no file extension; the real file may have been renamed to e.g. \code{.pdf} after
its body was fetched and its type sniffed. Given the path \code{\link[=cache_path]{cache_path()}}
returned, this resolves the actual file on disk:
}
\details{
\itemize{
\item the path itself when a non-empty file already exists there;
\item the single finalized sibling (same stem, real extension) when exactly one
exists for a \code{.x} placeholder;
\item \code{NA_character_} when nothing is cached yet (so the caller knows to
download) or when the match is ambiguous.
}
}
\examples{
\dontrun{
dest <- cache_path(url, "ee", "KMH-44")
resolve_cached_path(dest)
}
}
\seealso{
\code{\link[=cache_path]{cache_path()}}, \code{\link[=download_to_cache]{download_to_cache()}}
}
