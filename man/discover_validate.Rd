% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discover_validate.R
\name{discover_validate}
\alias{discover_validate}
\title{Validate a candidate (record, URL) pair against a downloaded PDF.}
\usage{
discover_validate(
  record,
  pdf_path,
  cfg,
  relevance_model = NULL,
  semantic_threshold = 0.5
)
}
\arguments{
\item{record}{A 1-row tibble in the planscanR result shape.}

\item{pdf_path}{Path to a local copy of the candidate PDF.}

\item{cfg}{Country discovery config (e.g. \code{\link[=at_discovery_config]{at_discovery_config()}}).}

\item{relevance_model}{Optional embedding model for the semantic signal.
When \code{NULL}, the semantic signal is skipped (which means signals 1-3
must carry the validation).}

\item{semantic_threshold}{Cosine-similarity threshold for signal 4.}
}
\value{
A list with: \code{passed} (logical), \code{signals} (named logical),
\code{notes} (character), \code{text} (the extracted first-10-pages text, used
downstream when the candidate is promoted to the sidecar).
}
\description{
Runs four signals in order:
}
\details{
\enumerate{
\item \strong{Aktenzahl exact} — the country's primary identifier appears verbatim
in the PDF text. Strongest signal.
\item \strong{Title overlap} — at least 2 content-bearing tokens (>= 5 chars)
from the record title appear in the PDF text after diacritic + German
digraph normalisation.
\item \strong{Extra-signal presence} — at least one of the country config's
\code{extra_signals} (e.g. \code{"UVP-G 2000"}) is in the PDF, AND the title
overlap signal also triggered with at least 1 token.
\item \strong{Semantic backup} — cosine similarity between first-10-pages text
and the record's title+summary clears \code{semantic_threshold} (default
0.5), using the relevance model. Only fires if none of the above did.
}

Any single signal passing is sufficient. The result reports which signal(s)
fired so calling code can apply stricter precision policies if it wants.
}
