% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discover_attachments.R
\name{discover_attachments}
\alias{discover_attachments}
\title{Discover attachments for records the portal returned empty.}
\usage{
discover_attachments(
  records,
  backend = NULL,
  country_config = NULL,
  relevance_model = NULL,
  queries_per_record = 3L,
  max_pdfs_per_record = 10L,
  max_results_per_query = 10L,
  cache_dir = NULL,
  max_file_size_mb = NULL,
  dry_run = FALSE,
  skip_if_attached = TRUE,
  semantic_threshold = 0.5
)
}
\arguments{
\item{records}{A tibble of records (e.g. from \code{\link[=get_assessments]{get_assessments()}} or
\code{\link[=index_cache]{index_cache()}}). All rows must share the same \code{country}.}

\item{backend}{A \code{\link[=search_backend]{search_backend()}}. Defaults to
\code{\link[=search_backend_tavily]{search_backend_tavily()}} (requires \code{TAVILY_API_KEY}).}

\item{country_config}{Per-country discovery config (e.g.
\code{\link[=at_discovery_config]{at_discovery_config()}}). When \code{NULL}, resolved from \code{records$country}.}

\item{relevance_model}{Optional embedding model for the validator's
semantic backup signal. When \code{NULL}, the validator runs without
semantic check.}

\item{queries_per_record}{Cap on the number of templates fired per
record. Lower = cheaper, less recall.}

\item{max_pdfs_per_record}{Cap on the number of validated PDFs kept per
record.}

\item{max_results_per_query}{Pass-through to the backend's \code{max_results}.}

\item{cache_dir}{Optional cache root. Defaults to
\code{getOption("planscanR.cache_dir")}.}

\item{max_file_size_mb}{Per-PDF size cap. Defaults to the package option.}

\item{dry_run}{If \code{TRUE}, runs phases 1-2 but skips ingest (no sidecar
writes, no PDFs copied into the cache). The returned tibble carries the
would-be discoveries in its \code{discovery_log} column for inspection.}

\item{skip_if_attached}{If \code{TRUE} (default), records that already have
\code{length(attachment_urls) > 0} are skipped — discovery is only for
records the portal returned empty.}

\item{semantic_threshold}{Cosine-similarity threshold for the validator's
semantic backup signal.}
}
\value{
The input tibble with \code{attachment_urls} and \code{local_path}
augmented for records that got new validated PDFs, plus a
\code{discovery_log} list-column with one entry per (record, query) attempt.
}
\description{
For each input record, this function:
}
\details{
\enumerate{
\item \strong{Discovers} — generates a small set of search queries from the
record's metadata and runs them through a \code{\link[=search_backend]{search_backend()}}.
\item \strong{Validates} — for each candidate URL that points at a PDF, downloads
a copy and runs \code{\link[=discover_validate]{discover_validate()}} against the record. Failed
candidates are discarded from disk.
\item \strong{Ingests} — for PDFs that pass, copies them into the canonical cache
layout (\verb{<cache>/files/<country>/<document_id>/}) and appends to the
record's sidecar JSON with \code{source = "discovery"} plus a
\code{discovery_log[]} audit entry.
}

This is the package's escape hatch for portals that do not expose
document URLs to anonymous callers. The Austrian UVP-DB is the canonical
example — see \code{\link[=get_assessments_at]{get_assessments_at()}} — but the function is portal-agnostic
once a country supplies a discovery config.
}
\seealso{
\code{\link[=search_backend_tavily]{search_backend_tavily()}}, \code{\link[=discover_validate]{discover_validate()}},
\code{\link[=at_discovery_config]{at_discovery_config()}}.
}
