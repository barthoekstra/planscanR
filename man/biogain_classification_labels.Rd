% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_assessments.R
\name{biogain_classification_labels}
\alias{biogain_classification_labels}
\title{Candidate labels for BIOGAIN zero-shot classification.}
\usage{
biogain_classification_labels()
}
\value{
A named character vector with a \code{"relevant"} attribute.
}
\description{
A named character vector: names are stable slugs (used as column suffixes,
e.g. \code{class_score_wind}), values are the natural-language hypotheses fed to
the zero-shot model. The set is \strong{mutually exclusive} (used with
\code{multi_label = FALSE}): a record is assigned its single best label.
}
\details{
Crucially it mixes positive energy classes with explicit \strong{negative}
classes (water management, general land-use / spatial planning, other
non-energy). A record whose best label is a negative class is classified
as not-relevant — this is what removes the \code{Flurneuordnung} /
\code{Wasserwirtschaft} / business-park records that clear a cosine 0.5 on the
noisy \code{renewable_zoning} topic.

The \code{relevant} attribute lists which slugs count as BIOGAIN-relevant.
}
\examples{
biogain_classification_labels()
attr(biogain_classification_labels(), "relevant")
}
