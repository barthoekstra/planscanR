% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_sidecar.R
\name{index_cache}
\alias{index_cache}
\title{Walk a planscanR cache and reconstruct a tibble from every sidecar.}
\usage{
index_cache(cache_dir = NULL, country = NULL)
}
\arguments{
\item{cache_dir}{Optional cache root. Defaults to
\code{tools::R_user_dir("planscanR", "cache")}.}

\item{country}{Optional ISO-2 country code to filter by. \code{NULL} returns all.}
}
\value{
A tibble in the planscanR schema, possibly with zero rows.
}
\description{
Lets you re-index a previously-populated cache without going back to any
portal. Useful when:
\itemize{
\item you've downloaded a large slice and want a quick offline tibble of it,
\item you've manually flattened or relocated files,
\item you want to enumerate what's already on disk before deciding what else
to fetch.
}
}
\examples{
\dontrun{
# Re-index everything currently in the cache
index_cache()

# Re-index just the Dutch records
index_cache(country = "nl")
}
}
