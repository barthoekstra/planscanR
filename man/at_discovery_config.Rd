% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/at_discovery_config.R
\name{at_discovery_config}
\alias{at_discovery_config}
\title{Discovery configuration for the Austrian UVP-DB.}
\usage{
at_discovery_config()
}
\value{
A list with the following named elements:
\itemize{
\item \code{query_templates} — list of function(record) -> character, each
producing one search query (or NULL to skip).
\item \code{state_domains} — named list \verb{bundesland -> character vector} of
authority domains to scope queries to.
\item \code{aktenzahl_regex} — regex that detects the country's primary
identifier in PDF text. For AT it matches the UBA-internal AZ
\verb{02 NNNN} plus a couple of dash/no-space variants.
\item \code{extract_proponent} — function(record) -> character, pulls the
proponent name out of the record's summary (returns NA on failure).
\item \code{extra_signals} — character vector of additional patterns the
validator will check for in PDF text (e.g. legal-basis strings).
}
}
\description{
Returns a named list of inputs that \code{\link[=discover_attachments]{discover_attachments()}} uses to
build queries for AT records and to validate candidate PDFs. AT-specific
rationale lives next to each field; the keys themselves are part of the
discovery-config contract that every country should provide.
}
\examples{
\dontrun{
cfg <- at_discovery_config()
rec <- index_cache(country = "at")[1, ]
lapply(cfg$query_templates, function(f) f(rec))
}
}
