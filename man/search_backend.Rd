% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discover_backend.R
\name{search_backend}
\alias{search_backend}
\title{Build a custom search backend.}
\usage{
search_backend(name, search_fn)
}
\arguments{
\item{name}{Short identifier for the backend (e.g. \code{"tavily"},
\code{"google-cse"}). Surfaced in logs and the sidecar \code{discovery_log[]}.}

\item{search_fn}{A function \verb{function(query, include_domains, max_results)}
returning a list of result objects, each a named list with at minimum
\code{url} (character) and optionally \code{title}, \code{content}, \code{score}.}
}
\value{
A \code{planscanR_search_backend} object.
}
\description{
Wraps a user-supplied search function as a \code{planscanR_search_backend}
object compatible with \code{\link[=discover_attachments]{discover_attachments()}}. Use this when you have an
in-house search service, want to swap to Bing / Brave / Google CSE without
waiting for first-party support, or need a deterministic mock in tests.
}
\examples{
\dontrun{
my_backend <- search_backend(
  name = "fake-google",
  search_fn = function(query, include_domains, max_results) {
    list(list(url = "https://example.org/x.pdf", title = "Test", score = 1))
  }
)
web_search(my_backend, "windpark parndorf", NULL, 5)
}
}
