% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_assessments_gb.R
\name{get_assessments_gb}
\alias{get_assessments_gb}
\title{Fetch environmental-assessment records from the United Kingdom.}
\usage{
get_assessments_gb(
  date_range = NULL,
  limit = Inf,
  download = FALSE,
  cache_dir = NULL,
  overwrite = FALSE,
  max_file_size_mb = NULL,
  write_sidecar = TRUE,
  refresh = FALSE,
  topic = NULL,
  relevance_threshold = NULL,
  relevance_model = NULL,
  query = NULL,
  status = NULL,
  ...
)
}
\arguments{
\item{date_range, limit, download, cache_dir, overwrite, max_file_size_mb, write_sidecar, refresh, topic, relevance_threshold, relevance_model}{See \code{\link[=get_assessments]{get_assessments()}}.}

\item{query}{Optional free-text query. Applied as a client-side
case-insensitive substring match on the project name.}

\item{status}{Optional project \emph{Stage} (client-side, case-insensitive).}

\item{...}{Reserved for future extensions; unused arguments are warned
about.}
}
\value{
A tibble; see \code{\link[=get_assessments]{get_assessments()}} for the required schema.
}
\description{
Implementation of \code{\link[=get_assessments]{get_assessments()}} for the United Kingdom. Backed by the
Planning Inspectorate's \strong{National Infrastructure Consenting} service
(\url{https://national-infrastructure-consenting.planninginspectorate.gov.uk/}),
the register of \strong{Nationally Significant Infrastructure Projects (NSIPs)}.
Every NSIP application carries a statutory \strong{Environmental Statement (ES)},
so the register is an EIA-equivalent source; each row is therefore an
EIA-equivalent procedure and there is no \code{assessment_type} selector (this is
a single-register handler).
}
\details{
Scope is \strong{NSIP only}. Local-authority / Town-and-Country-Planning EIAs
(the much larger body of smaller English/Welsh consents) are \emph{not} in this
register and are out of scope here.
}
\section{URL enumeration}{

The whole register is published as a single bulk CSV export at
\verb{…/api/applications-download} (≈540 rows). One GET fetches the entire
register; it is parsed with base \code{\link[utils:read.table]{utils::read.csv()}} (no new package
dependency). The handler's page generator returns every row on its first
call and then signals exhausted, respecting the global \code{limit}. The
canonical record URL is the project landing page
\verb{…/projects/\{REF\}} where \code{{REF}} is the \emph{Project reference} (e.g.
\code{EN010001}), which is the clean, path-safe, unique \code{document_id}.
}

\section{Attachments}{

Per record (sidecar-first via the cache) the handler fetches the project's
Environmental Statement document list
(\verb{…/projects/\{REF\}/documents?type=Environmental Statement}) and scrapes the
published-document PDF hrefs, which live on
\verb{https://nsip-documents.planninginspectorate.gov.uk/published-documents/\{REF\}-\{NNNNNN\}-\{title\}.pdf}.
These become \code{attachment_urls} (a flat list — no per-section split). A
project with no ES documents yet yields an empty \code{attachment_urls} vector,
which is valid.
}

\section{Geometry (point)}{

Each row carries an Ordnance Survey National Grid point (\emph{Grid reference -
Easting} / \emph{Northing}). When both are present, a point \code{.geometry.geojson}
is written next to the sidecar (GeoJSON \code{Point}, OSGB36 \strong{EPSG:27700}, with
the GeoJSON-2008 \code{crs} member naming \verb{urn:ogc:def:crs:EPSG::27700}).
\code{geometry_path} (relative to the cache root, per schema v3) and
\code{geometry_crs} (\code{"EPSG:27700"}) are recorded on the sidecar. Coordinates are
kept in the source CRS — reproject downstream with \code{sf} if you need WGS84.
The raw easting/northing and the portal's WGS84 \emph{GPS co-ordinates} string
are also surfaced as extras.
}

\section{Filter coverage (v0.1)}{

Every filter is applied \strong{client-side} (the bulk export is unfiltered):
\itemize{
\item \code{query} — case-insensitive substring match on \code{title} (the project name).
\item \code{date_range} — matched against \code{date_decision} when present, otherwise
\code{date_published} (the application-accepted date).
\item \code{status} — case-insensitive match on the portal \emph{Stage} (e.g.
\code{"Pre-application"}, \code{"Examination"}, \code{"Post-decision"}, \code{"Withdrawn"}).
\item \code{limit} — caps the total number of records returned.
}
}

\section{Performance}{

Enumeration is a single bulk-CSV GET plus one document-list fetch per record
(sidecar-first, so repeat runs are fast). To honour the portal's
\code{robots.txt} \code{Crawl-delay: 10}, GB requests are throttled to 0.1 requests
per second by default (one request every 10 s); override via
\code{getOption("planscanR.gb_throttle_rate")} (requests/sec; falsy disables).
The package's neutral \verb{planscanR/…} User-Agent is allowed by the portal's
\code{robots.txt} (AI-crawler agents such as ClaudeBot / GPTBot / CCBot are
\verb{Disallow: /}).
}

\examples{
\dontrun{
# Quick smoke test
get_assessments_gb(limit = 3, download = FALSE)

# Substring query on the project name
get_assessments_gb(query = "solar", limit = 20, download = FALSE)

# Only projects at the Examination stage
get_assessments_gb(status = "Examination", limit = 20, download = FALSE)

# Date range (matched against the decision / acceptance date)
get_assessments_gb(
  date_range = c("2020-01-01", "2020-12-31"),
  limit = 20,
  download = FALSE
)
}
}
\seealso{
\code{\link[=get_assessments]{get_assessments()}}, \code{\link[=get_assessments_coverage]{get_assessments_coverage()}}.
}
