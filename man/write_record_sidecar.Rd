% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_sidecar.R
\name{write_record_sidecar}
\alias{write_record_sidecar}
\title{Write a sidecar JSON for a single record.}
\usage{
write_record_sidecar(record, downloads = NULL, root = NULL)
}
\arguments{
\item{record}{A 1-row tibble in the planscanR result shape.}

\item{downloads}{The structured download-status tibble produced by
\code{download_attachments()}. May be empty when \code{download = FALSE}.}

\item{root}{Cache root (or \code{NULL} to use the default).}
}
\value{
Path to the written sidecar, invisibly.
}
\description{
Atomic write: data is serialised to a temp file in the target dir and then
renamed over the destination, so a crash mid-write cannot leave a
half-written sidecar in place.
}
\details{
This is part of the sidecar I/O contract between \code{planscanR} (the cache
owner) and the downstream family packages: \code{planscanR.screen} and
\code{planscanR.biogain} persist their derived columns (relevance / class scores,
review translations) by calling \code{planscanR::write_record_sidecar()}. The
merge logic that preserves existing \code{files[]} / \code{relevance_scores[]} /
\verb{class_*} arrays lives here, so adding a new score family never wipes
existing ones.
}
