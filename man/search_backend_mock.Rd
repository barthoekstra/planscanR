% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discover_backend_mock.R
\name{search_backend_mock}
\alias{search_backend_mock}
\title{Build an in-memory mock search backend.}
\usage{
search_backend_mock(responses = list())
}
\arguments{
\item{responses}{Named list. Names are queries (matched exactly first,
then by \code{grepl()} substring) and values are lists of result objects
(each \code{list(url, title, content, score)}). Names not used are matched
via the special key \code{"_default"} if supplied; absent that, an unmatched
query yields an empty list.}
}
\value{
A \code{planscanR_search_backend}.
}
\description{
Returns a \code{planscanR_search_backend} that looks up incoming queries in a
static map and returns the canned responses. Used by the test suite (no
live HTTP) and by \code{\link[=discover_attachments]{discover_attachments()}} when you want a fully
deterministic replay against a cached corpus.
}
\examples{
\dontrun{
mock <- search_backend_mock(list(
  "windpark parndorf" = list(
    list(url = "https://x.example/parndorf.pdf", title = "Bescheid")
  ),
  "_default" = list()
))
web_search(mock, "windpark parndorf")
}
}
