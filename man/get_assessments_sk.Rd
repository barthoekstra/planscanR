% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_assessments_sk.R
\name{get_assessments_sk}
\alias{get_assessments_sk}
\title{Fetch environmental-assessment records from Slovakia.}
\usage{
get_assessments_sk(
  date_range = NULL,
  limit = Inf,
  download = FALSE,
  cache_dir = NULL,
  overwrite = FALSE,
  max_file_size_mb = NULL,
  write_sidecar = TRUE,
  refresh = FALSE,
  topic = NULL,
  relevance_threshold = NULL,
  relevance_model = NULL,
  query = NULL,
  assessment_type = "All",
  ...
)
}
\arguments{
\item{date_range, limit, download, cache_dir, overwrite, max_file_size_mb, write_sidecar, refresh, topic, relevance_threshold, relevance_model}{See \code{\link[=get_assessments]{get_assessments()}}.}

\item{query}{Optional free-text query, matched client-side as a
case-insensitive substring of the record title.}

\item{assessment_type}{One of \code{"All"} (default), \code{"EIA"}, or \code{"SEA"}.
Filters the mixed \code{eia_projects} list client-side by the \code{zbierka}-derived
type.}

\item{...}{Reserved for future extensions; unused arguments are warned
about.}
}
\value{
A tibble; see \code{\link[=get_assessments]{get_assessments()}} for the required schema.
}
\description{
Implementation of \code{\link[=get_assessments]{get_assessments()}} for Slovakia. Backed by the Slovak
EIA/SEA central information system \emph{enviroportal.sk}
(\url{https://www.enviroportal.sk/}), a React single-page app served by a
Symfony \strong{API Platform} JSON backend. The handler talks to that JSON API
directly (pure \code{httr2}, no browser) with the \code{Accept: application/ld+json}
content negotiation header.
}
\details{
The portal publishes a single \code{eia_projects} collection that mixes
project-level EIA and plan/programme SEA records; the \code{zbierka} (law
collection) string carries the discriminator (\code{"časť EIA"} vs
\code{"časť SEA"}). The handler tags each row with an \code{assessment_type}
column (\code{"EIA"} / \code{"SEA"}, derived from \code{zbierka}) and filters client-side
when an \code{assessment_type} argument is supplied; a \code{register} column carries
the raw collection label (\code{"eia_projects"}). \code{document_id} is the globally
unique numeric portal \code{id}, prefixed \code{"SK-"} (e.g. \code{"SK-171227"}), so records
never collide on disk.
}
\section{URL enumeration}{

The list endpoint is \code{GET /api/eia_projects?page=N} (with
\code{Accept: application/ld+json}). Its \code{hydra:member} is an \strong{array of arrays}:
a bucket of record objects, a pagination-metadata object, and (sometimes) an
empty bucket. The handler flattens one level and keeps only the record
objects (those with a \code{seoId}). \code{hydra:totalItems} / \code{hydra:view} are
unreliable or absent, so the page generator paginates \verb{page = 1, 2, 3, …}
\strong{until a page flattens to zero records} (the seam \code{sk_fetch_search()},
parsed with \code{perform_json} after attaching the \code{ld+json} Accept header). The
canonical detail URL for each record is the human SPA page
\verb{https://www.enviroportal.sk/eia/detail/\{seoId\}}; the API detail path is
\verb{/api/eia_projects/\{seoId\}}.
}

\section{Attachments}{

Each record's detail JSON carries \code{dokumenty$data}, an array of \strong{step
groups} (\verb{\{step, number, items\}}) where the \code{step} names the procedural
phase. Each \code{items[]} element is either a document group (\verb{\{title, items:[ \{type, label, id, url, …\} ]\}}) or a plain text field (\verb{\{type:"text", …\}}).
The handler collects every leaf whose \code{type} is a downloadable file
(\code{"PDF"}, etc.), absolutises its \code{url} (\verb{/eia/dokument/\{id\}} →
\verb{https://www.enviroportal.sk/eia/dokument/\{id\}}), and groups the URLs by the
\code{step} (phase) name into per-section \verb{attachment_urls_<slug>} columns (the
DE / IT pattern; the slug is the Slovak-transliterated, ASCII-folded step)
plus the deduplicated union in \code{attachment_urls}. A record with no
downloadable documents yields an empty \code{attachment_urls} vector, which is
valid.
}

\section{Filter coverage (v0.1)}{

\itemize{
\item \code{assessment_type} — \code{"All"} (default), \code{"EIA"}, or \code{"SEA"}. The API serves
one mixed list, so each record is tagged from \code{zbierka} and the filter is
applied here in R.
\item \code{query} — matched client-side as a case-insensitive substring of the
record title.
\item \code{date_range} — matched client-side against \code{date_published} (the date part
of the portal's \emph{datumPoslednejZmeny} / last-change timestamp).
\code{date_decision} is always \code{NA} (the API exposes no separate decision
date).
\item \code{limit} — caps the total number of records returned.
}
}

\section{Performance}{

The register is large (~18 700 records across ~940 pages of 20). A cold crawl
is one list GET per page plus one detail GET per record (sidecar-first, so
repeat runs are fast). A \code{limit} keeps exploratory runs bounded. To be polite
to the shared government portal, SK requests are throttled to 5 requests per
second by default; override via \code{getOption("planscanR.sk_throttle_rate")}
(requests/sec; falsy disables).
}

\examples{
\dontrun{
# Quick smoke test
get_assessments_sk(limit = 3, download = FALSE)

# SEA only
get_assessments_sk(assessment_type = "SEA", limit = 10, download = FALSE)

# Title substring
get_assessments_sk(query = "skladov", limit = 20, download = FALSE)
}
}
\seealso{
\code{\link[=get_assessments]{get_assessments()}}, \code{\link[=get_assessments_coverage]{get_assessments_coverage()}}.
}
