% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_assessments.R
\name{select_assessments}
\alias{select_assessments}
\title{Apply the BIOGAIN selection rule to scored + classified records.}
\usage{
select_assessments(
  records,
  topics = biogain_assessment_topics(),
  relevance_threshold = 0.5,
  kw_min = 2L,
  nonrenewable = c("fossil_power", "oil_gas_extraction", "nuclear"),
  nonrenewable_score = 0.5
)
}
\arguments{
\item{records}{A tibble that has been scored (\verb{relevance_score_<slug>}
columns) and classified (\code{class_label}, \code{class_relevant}, \code{class_score}).
Keyword columns (\code{kw_total}) are computed via \code{\link[=score_keywords]{score_keywords()}} if absent.}

\item{topics}{Named topic vector whose slugs name the cosine columns to
consider. Defaults to \code{\link[=biogain_assessment_topics]{biogain_assessment_topics()}}.}

\item{relevance_threshold}{Cosine cutoff; a record is cosine-relevant if any
topic clears it. Default \code{0.5}.}

\item{kw_min}{Minimum \code{kw_total} for the keyword arm to fire. Default \code{2}.}

\item{nonrenewable}{Classifier labels treated as confidently-off-target when
their \code{class_score} clears \code{nonrenewable_score}.}

\item{nonrenewable_score}{Confidence cutoff for the non-renewable trim.}
}
\value{
\code{records} with a logical \code{selected} column added (and \code{cosine_max},
\code{cosine_relevant}, and keyword columns if they were not already present).
}
\description{
Combines the three relevance signals into one decision. A record is
\strong{selected} when any one of the signals fires —
}
\details{
\itemize{
\item the embedding cosine score clears the relevance threshold, OR
\item the classifier labels it as a relevant (renewable-energy) type, OR
\item it contains at least \code{kw_min} keyword hits —
}

\emph{and} it is not confidently off-target (not classified as fossil power,
oil/gas extraction, or nuclear above \code{nonrenewable_score}).

Rationale (from the NL analysis): the three signals are complementary — the
cosine score misses some near-threshold energy records, the classifier
mislabels some into negative classes, and the keyword layer catches explicit
mentions the other two dilute. Taking their union favours recall (this step
runs before acquisition; precision can still be improved downstream), while
the fossil/oil-gas/nuclear trim drops records that are clearly off-target for
BIOGAIN.
}
\examples{
\dontrun{
recs <- classify_assessments(index_cache(country = "nl"))
sel <- select_assessments(recs)
table(sel$selected)
}
}
