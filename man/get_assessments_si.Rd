% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_assessments_si.R
\name{get_assessments_si}
\alias{get_assessments_si}
\title{Fetch environmental-assessment records from Slovenia.}
\usage{
get_assessments_si(
  date_range = NULL,
  limit = Inf,
  download = FALSE,
  cache_dir = NULL,
  overwrite = FALSE,
  max_file_size_mb = NULL,
  write_sidecar = TRUE,
  refresh = FALSE,
  topic = NULL,
  relevance_threshold = NULL,
  relevance_model = NULL,
  assessment_type = "All",
  ...
)
}
\arguments{
\item{date_range, limit, download, cache_dir, overwrite, max_file_size_mb, write_sidecar, refresh, topic, relevance_threshold, relevance_model}{See \code{\link[=get_assessments]{get_assessments()}}.}

\item{assessment_type}{One of \code{"All"} (default), \code{"EIA"}, or \code{"SEA"}.
Selects which register(s) to crawl.}

\item{...}{Reserved for future extensions; unused arguments are warned
about.}
}
\value{
A tibble; see \code{\link[=get_assessments]{get_assessments()}} for the required schema.
}
\description{
Implementation of \code{\link[=get_assessments]{get_assessments()}} for Slovenia. Backed by the
Slovenian government portal gov.si (\url{https://www.gov.si/}), which
publishes the environmental-assessment registers as bulk JSON exports
under \url{https://www.gov.si/podrocja/okolje-in-prostor/okolje/okoljske-presoje/}.
Three adjacent registers are merged into a single result tibble:
}
\details{
\itemize{
\item \strong{Predhodni postopek} — the EIA screening register
(\code{predhodni-postopek}).
\item \strong{CPVO — državni prostorski načrti} — SEA decisions for state spatial
plans (\code{cpvo-drzavni}).
\item \strong{CPVO — občinski prostorski načrti} — SEA decisions for municipal
spatial plans (\code{cpvo-obcinski}).
}

An \code{assessment_type} column (\code{"EIA"} for the screening register, \code{"SEA"}
for the two CPVO registers) tags each row and is preserved in the offline
metadata cache so downstream tooling can tell them apart without re-fetching
anything; a \code{register} column carries the raw portal register label.
\code{document_id} is prefixed with \code{"PRED-"} / \code{"CPVO-DRZ-"} / \code{"CPVO-OBC-"}
so the three registers never collide on disk.
}
\section{URL enumeration}{

Each register exposes a single bulk JSON export at
\verb{<list base URL><list>/export/json/}, returning the whole register as one
JSON array (NOT paginated). The handler issues one GET per register and
parses every element of the array, respecting the global \code{limit}. The
canonical detail URL for each record is \verb{<list base URL><URLSegment>/}.
}

\section{Attachments}{

The bulk-export document ids do not map to filenames, so attachments are
scraped from each record's detail page (sidecar-first via the cache).
The handler fetches the detail HTML, collects every \verb{a[href^="/assets/seznami/"]}
link, and absolutises it against \verb{https://www.gov.si}. These become
\code{attachment_urls} (a flat list — no per-section split). A record with no
such links yields an empty \code{attachment_urls} vector, which is valid.
}

\section{Filter coverage (v0.1)}{

\itemize{
\item \code{assessment_type} — selects which register(s) to crawl: \code{"All"}
(default), \code{"EIA"} (screening only), or \code{"SEA"} (both CPVO registers).
Applied here in R, not server-side (the bulk exports are unfiltered).
\item \code{date_range} — matched client-side against \code{date_published} (the
portal's \emph{Datum objave} / \emph{Datum} field). \code{date_decision} is always
\code{NA} because the portal exposes no separate decision date as a date.
\item \code{limit} — caps the total number of records returned across all crawled
registers.
}
}

\section{Performance}{

The registers are small (a few hundred screening records, a few dozen CPVO
records). A cold crawl is one bulk-export GET per register plus one
detail-page fetch per record (sidecar-first, so repeat runs are fast). To
be polite to the shared government portal, SI requests are throttled to 5
requests per second by default; override via
\code{getOption("planscanR.si_throttle_rate")} (requests/sec; falsy disables).
The register covers roughly 2021 onward.
}

\examples{
\dontrun{
# Quick smoke test
get_assessments_si(limit = 3, download = FALSE)

# SEA only (both CPVO registers)
get_assessments_si(assessment_type = "SEA", limit = 10, download = FALSE)

# Date range
get_assessments_si(
  date_range = c("2021-01-01", "2021-12-31"),
  limit = 20,
  download = FALSE
)
}
}
\seealso{
\code{\link[=get_assessments]{get_assessments()}}, \code{\link[=get_assessments_coverage]{get_assessments_coverage()}}.
}
