% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biogain_review_app.R
\name{run_biogain_review}
\alias{run_biogain_review}
\title{Launch the BIOGAIN review and pipeline-funnel app}
\usage{
run_biogain_review(
  cache_dir = NULL,
  data_dir = NULL,
  launch.browser = interactive(),
  ...
)
}
\arguments{
\item{cache_dir}{Sidecar cache root to read. \code{NULL} (default) uses
\code{PLANSCANR_CACHE}, then \code{getOption("planscanR.cache_dir")}, then the
package default (\code{\link[tools:userdir]{tools::R_user_dir()}} \code{"cache"}).}

\item{data_dir}{Writable directory for the app's snapshot, \code{reviews.csv}, and
reviewers list. \code{NULL} (default) uses the \strong{cache root} (\code{cache_dir}), so
the human annotations sit alongside the data they describe and travel with
any cache sync. They are written at the root, not under \verb{files/}, so
\code{\link[=clear_cache]{clear_cache()}} leaves them intact.}

\item{launch.browser}{Passed to \code{\link[shiny:runApp]{shiny::runApp()}}; defaults to
\code{\link[=interactive]{interactive()}}.}

\item{...}{Further arguments forwarded to \code{\link[shiny:runApp]{shiny::runApp()}} (e.g. \code{port},
\code{host}).}
}
\value{
Invisibly, the value of \code{\link[shiny:runApp]{shiny::runApp()}} (called for its side
effect of running the app).
}
\description{
A Shiny app for the BIOGAIN project that visualises how environmental-
assessment records flow through the planscanR pipeline (indexed -> embedding
cosine / zero-shot classifier / keyword -> ensemble selection -> downloaded)
and lets a reviewer build a human ground-truth selection to compare the
automated pre-selection against. It reads the sidecar cache \strong{read-only} via
\code{\link[=index_cache]{index_cache()}} / \code{\link[=select_assessments]{select_assessments()}} and keeps its own review decisions,
offline translations, and a cached snapshot in a separate user directory.
}
\details{
Features: an interactive funnel + per-country breakdown, automated-vs-human
agreement metrics (precision / recall / F1), a sortable review table with
per-row and bulk keep/drop/unsure, an unbiased stratified random-sample
reviewer, a single-record stepper (arrow-key navigation, decide-and-advance),
required reviewer attribution, and on-demand offline English translation of
titles/summaries.

Requires the optional packages \pkg{shiny}, \pkg{bslib},
\pkg{reactable}, \pkg{plotly}, \pkg{htmltools}, and \pkg{jsonlite}.
Title/summary translation uses the Python \code{argostranslate} package via
\pkg{reticulate}; its language-pair models download once on first use.
}
\examples{
\dontrun{
# Uses the package cache + per-user data dir:
run_biogain_review()

# Point at a specific cache and a fixed port:
run_biogain_review(cache_dir = "/path/to/plans", port = 7654)
}
}
