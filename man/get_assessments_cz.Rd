% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_assessments_cz.R
\name{get_assessments_cz}
\alias{get_assessments_cz}
\title{Fetch environmental-assessment records from the Czech Republic.}
\usage{
get_assessments_cz(
  date_range = NULL,
  limit = Inf,
  download = FALSE,
  cache_dir = NULL,
  overwrite = FALSE,
  max_file_size_mb = NULL,
  write_sidecar = TRUE,
  refresh = FALSE,
  topic = NULL,
  relevance_threshold = NULL,
  relevance_model = NULL,
  assessment_type = "All",
  ...
)
}
\arguments{
\item{date_range, limit, download, cache_dir, overwrite, max_file_size_mb, write_sidecar, refresh, topic, relevance_threshold, relevance_model}{See \code{\link[=get_assessments]{get_assessments()}}.}

\item{assessment_type}{One of \code{"All"} (default), \code{"EIA"}, or \code{"SEA"}.
Selects which domestic register(s) to crawl.}

\item{...}{Reserved for future extensions; unused arguments are warned
about.}
}
\value{
A tibble; see \code{\link[=get_assessments]{get_assessments()}} for the required schema.
}
\description{
Implementation of \code{\link[=get_assessments]{get_assessments()}} for the Czech Republic. Backed by
CENIA's \emph{Informační systém EIA/SEA} (\url{https://portal.cenia.cz/eiasea/}), a
server-rendered JSP application (Apache Tomcat — not a SPA). Two \strong{domestic}
registers are crawled:
}
\details{
\itemize{
\item \strong{EIA} — \emph{Záměry na území ČR} (project-level EIA on Czech territory),
\verb{https://portal.cenia.cz/eiasea/view/eia100_cr}.
\item \strong{SEA} — \emph{Posuzování koncepcí} (concept / plan SEA),
\verb{https://portal.cenia.cz/eiasea/view/SEA100_koncepce}.
}

Both registers are merged into a single result tibble; an
\code{assessment_type} column (\code{"EIA"} for \emph{Záměry na území ČR}, \code{"SEA"} for
\emph{Posuzování koncepcí}) tags each row and is round-tripped to the sidecar so
downstream tooling can tell them apart without re-fetching anything.
\code{document_id} is the portal's register-namespaced detail code, e.g.
\code{"EIA_JHC1237"} / \code{"SEA_HKK015K"}, so the two registers never collide on
disk (no extra prefix is added).
}
\section{Scope (domestic CZ only)}{

This handler deliberately crawls \strong{only} the two domestic registers above.
The portal also hosts cross-border / foreign registers and several special
sub-registers (\code{eia100_mimo_cr} — projects outside CZ, \code{sea100_mezistatni}
— cross-border SEA, the sub-limit / priority-transport / large-project EIA
sub-registers \code{eia100_podlimitni} / \verb{_pdz} / \verb{_vzvp} / \code{eia244}, and the
territorial-planning SEA sub-registers \verb{sea100_pur*} / \verb{zur*} / \verb{up*}).
Those are \strong{never} enumerated. Note that ministry-coded records (\verb{EIA_MZP*},
\verb{SEA_MZP*}) inside the two domestic registers \emph{are} in scope — they are
domestic projects/plans assessed by the Ministry, not foreign ones. The two
in-scope view codes are hard-coded.
}

\section{URL enumeration}{

Index listings paginate via a \strong{1-based} \code{p} query parameter (10 records
per page); each page is one HTML GET listing the detail code, project/plan
title, competent authority (\emph{Příslušný úřad}), annex category (\emph{Zařazení}),
last-modified timestamp (\emph{Změněno}), and status (\emph{Stav}). Detail pages live
at \verb{/eiasea/detail/EIA_<CODE>} and \verb{/eiasea/detail/SEA_<CODE>} and carry
every field a downstream classifier needs. Out-of-range page indices are
\strong{clamped to the last page} by the server (they return the last page's rows
again rather than an empty page), so pagination stops when a page contributes
no detail codes that were not already seen.
}

\section{Geometry}{

The CENIA detail pages expose \strong{no} coordinates or GeoJSON — location is
administrative text only (\emph{Kraj} / \emph{Okres} / \emph{Obec} / \emph{Katastr}). No geometry
columns are emitted.
}

\section{Attachments}{

Detail pages render document links inline inside the detail table's value
cells as \verb{<a class="entity_field" href="/eiasea/download/<token>/<file>">}.
Both the base64-ish token and the trailing human filename come straight out
of the rendered href, so the handler captures the full href verbatim rather
than reconstructing the token. Downloads are anonymous (no authentication).
Documents are grouped by process-stage headings (OZNÁMENÍ, ZJIŠŤOVACÍ
ŘÍZENÍ, DOKUMENTACE, POSUDEK, VEŘEJNÉ PROJEDNÁNÍ, STANOVISKO for EIA; the
oznámení / vyhodnocení / návrh koncepce / stanovisko / schválená koncepce
fields for SEA). The handler slugs the nearest stage heading / field label
into an \verb{attachment_urls_<slug>} / \verb{local_path_<slug>} list-column (Czech
diacritics transliterated to ASCII). The deduplicated union goes into
\code{attachment_urls} / \code{local_path} (required by the schema).

Some attachments are very large ZIP bundles (e.g. a 79 MB \code{oznameni.zip});
\code{\link[=get_assessments]{get_assessments()}}'s \code{max_file_size_mb} cap is honoured during download, so
oversized files are skipped and recorded as \code{"skipped_size"} rather than
fetched.
}

\section{Filter coverage (v0.1)}{

\itemize{
\item \code{assessment_type} — selects which register(s) to crawl: \code{"All"}
(default), \code{"EIA"} (\emph{Záměry na území ČR} only), or \code{"SEA"}
(\emph{Posuzování koncepcí} only). Applied here in R.
\item \code{date_range} — matched client-side against \code{date_published} (the
EIA \emph{Datum a čas posledních úprav} last-modified date, or the SEA
\emph{Datum zveřejnění} publication date). \code{date_decision} is \code{NA} — the
portal exposes no single clean decision-date field.
}
}

\section{Performance}{

The two registers are ~22,780 (EIA) + ~640 (SEA) records, so callers almost
always pass \code{limit}. To stay polite to the single Tomcat instance under a
large crawl, CZ requests are throttled to 2 requests per second by default.
Override via \code{getOption("planscanR.cz_throttle_rate")} (requests/sec; falsy
disables).
}

\section{Language}{

Record content is Czech (ISO-639-1 \code{cs}; note the country code \code{cz} differs
from the language code). The portal's \code{lang=en} switch only flips UI chrome,
not record content, so the handler fetches with \code{lang=cs}.
}

\examples{
\dontrun{
# Quick smoke test
get_assessments_cz(limit = 3, download = FALSE)

# SEA only
get_assessments_cz(assessment_type = "SEA", limit = 5, download = FALSE)
}
}
\seealso{
\code{\link[=get_assessments]{get_assessments()}}, \code{\link[=get_assessments_coverage]{get_assessments_coverage()}}.
}
