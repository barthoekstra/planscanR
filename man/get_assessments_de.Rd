% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_assessments_de.R
\name{get_assessments_de}
\alias{get_assessments_de}
\title{Fetch environmental-assessment records from Germany.}
\usage{
get_assessments_de(
  date_range = NULL,
  limit = Inf,
  download = TRUE,
  cache_dir = NULL,
  overwrite = FALSE,
  max_file_size_mb = NULL,
  write_sidecar = TRUE,
  refresh = FALSE,
  topic = NULL,
  relevance_threshold = NULL,
  relevance_model = NULL,
  query = NULL,
  jurisdiction = NULL,
  ...
)
}
\arguments{
\item{date_range}{Length-2 vector \code{c(from, to)} of dates or parseable
strings. Filters by \code{date_decision}.}

\item{limit}{Integer. Maximum records to return. Defaults to \code{Inf}; you
are strongly encouraged to set a small value (e.g. \code{50}) when exploring,
because a cold-cache full crawl enumerates all ~2,258 search pages.}

\item{download, cache_dir, overwrite, max_file_size_mb, write_sidecar, refresh}{See \code{\link[=get_assessments]{get_assessments()}}.}

\item{topic, relevance_threshold, relevance_model}{Forwarded from
\code{\link[=get_assessments]{get_assessments()}}. \code{relevance_threshold} \strong{only affects downloading}:
records below it keep their sidecar and their tibble row, only their PDFs
are skipped.}

\item{query}{Free-text search string. Sent server-side as \verb{q=<query>}.
When \code{NULL}, the wildcard \verb{q=*:*} is used, which enumerates the full
register (~24,289 records across ~2,429 search pages).}

\item{jurisdiction}{Character vector. Substring match on the
federal-state partner displayed on each detail page
(e.g. \code{jurisdiction = "Bayern"} keeps Bavarian records).}

\item{...}{Reserved for future extensions; unused arguments are warned about.}
}
\value{
A tibble; see \code{\link[=get_assessments]{get_assessments()}} for the schema.
}
\description{
Implementation of \code{\link[=get_assessments]{get_assessments()}} for Germany. Backed by the
UVP-Verbund portal at \url{https://www.uvp-verbund.de/}, a federated catalogue
of UVP (Umweltverträglichkeitsprüfung) procedures published by all
federal-state authorities. URL enumeration uses the portal's own
server-side full-text search (\verb{/freitextsuche}) — there is no XML sitemap.
Per-record metadata is parsed from each detail page with rvest.
}
\section{URL enumeration}{

The portal exposes no sitemap, no OAI-PMH, and no CSW endpoint. The only
enumeration route is the search interface itself:

\if{html}{\out{<div class="sourceCode">}}\preformatted{https://www.uvp-verbund.de/freitextsuche?q=<query>&toggle_procedure=&ranking=score&page=<n>
}\if{html}{\out{</div>}}

The portal is Solr-backed; when \code{query} is \code{NULL} we send the
match-everything wildcard \verb{q=*:*}, which today returns the full register
(~24,289 records) and paginates correctly. Earlier versions of this
handler fell back to \code{q=uvp} because \verb{q=*:*} rendered page 1 but then
returned empty pages from page 2 onwards; the portal has since fixed
that, and the wildcard is now the default.
\verb{toggle_procedure=} (empty value) is set explicitly: the portal's default
(\code{toggle_procedure=on}) restricts results to currently-running plus
last-year-modified procedures and silently drops ~80\% of historical
records.

On a cold cache, a full enumeration over ~2,429 pages is slow; users are
strongly encouraged to set \code{limit} (and ideally \code{query}) when exploring.
}

\section{Filter coverage (v0.1)}{

Only filters that map cleanly to portal-side parameters or to extractable
detail-page fields are active in this version:
\itemize{
\item \code{query} — passed straight through as the server-side \code{q} parameter
(real full-text search, not a client-side substring match).
\item \code{date_range} — matches against \code{date_decision}, which on this portal
carries the \strong{"Zuletzt geaendert"} date (last-modified timestamp shown
in the detail page header).
\item \code{jurisdiction} — substring match against the federal-state partner
(from \verb{div.teaser-logo-partner img[alt]}, e.g. \code{"Baden-Württemberg"}).
}

The portal's \verb{procedure=} facet (Zulassungsverfahren, Bauleitplanung,
etc.) is reserved for a future release.
}

\section{Attachments}{
 per-page section split:
UVP detail pages group documents under \code{h4.title-font} headings. The set of
headings is \strong{open-ended and discovered per page} rather than fixed: every
heading that carries documents becomes its own parallel list-column
\verb{attachment_urls_<slug>} / \verb{local_path_<slug>}, and the per-file \code{section}
tag is persisted in the sidecar JSON. Known headings get a stable, curated
slug; any other heading is auto-slugged from its title (German digraphs
transliterated to ASCII), so a newly-appearing section type is captured
without a code change.

Curated slugs (see the internal \code{de_section_map()}):
\itemize{
\item \code{uvp_bericht} — \emph{"UVP-Bericht, ggf. Antragsunterlagen"} (the UVP report
itself plus the applicant's project documents — the substantive documents
for downstream analysis).
\item \code{berichte} — \emph{"Berichte und Empfehlungen"} (technical reports and
recommendations).
\item \code{entscheidung} — \emph{"Entscheidung"} (the decision / Bescheid documents).
\item \code{auslegung} — \emph{"Auslegungsinformationen"} (public-consultation notices).
\item \code{weitere} — \emph{"Weitere Unterlagen"} (catch-all section; often very large).
}

\code{attachment_urls} / \code{local_path} are the deduplicated union across all
discovered sections, ordered curated-first (in the order above) then any
auto-slugged sections in page order. Required by the planscanR schema.

When \code{download = TRUE}, files in \strong{all} discovered sections are fetched —
subject to \code{max_file_size_mb} and the relevance threshold. (The
\code{data-raw/biogain_acquire.R} runbook can restrict downloads to a chosen
subset of sections; the handler itself always captures them all.)
}

\examples{
\dontrun{
# Quick smoke test
get_assessments_de(limit = 3, download = FALSE)

# Wind-energy search
get_assessments_de(query = "windenergie", limit = 20, download = FALSE)

# Date range
get_assessments_de(
  date_range = c("2024-01-01", "2024-12-31"),
  limit = 20,
  download = FALSE
)
}
}
\seealso{
\code{\link[=get_assessments]{get_assessments()}}, \code{\link[=get_assessments_coverage]{get_assessments_coverage()}}.
}
