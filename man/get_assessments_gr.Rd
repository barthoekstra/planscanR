% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_assessments_gr.R
\name{get_assessments_gr}
\alias{get_assessments_gr}
\title{Fetch environmental-assessment records from Greece.}
\usage{
get_assessments_gr(
  date_range = NULL,
  limit = Inf,
  download = FALSE,
  cache_dir = NULL,
  overwrite = FALSE,
  max_file_size_mb = NULL,
  write_sidecar = TRUE,
  refresh = FALSE,
  topic = NULL,
  relevance_threshold = NULL,
  relevance_model = NULL,
  query = NULL,
  type = NULL,
  ...
)
}
\arguments{
\item{date_range, limit, download, cache_dir, overwrite, max_file_size_mb, write_sidecar, refresh, topic, relevance_threshold, relevance_model}{See \code{\link[=get_assessments]{get_assessments()}}.}

\item{query}{Free-text query; sent server-side as \code{filter[text_search]}.}

\item{type}{Optional decision-type code (server-side \code{filter[type]}), e.g.
\code{"aepo_creation"}. See \code{eprm_gr_facets()} for the enum.}

\item{...}{Reserved for future extensions; unused arguments are warned about.}
}
\value{
A tibble; see \code{\link[=get_assessments]{get_assessments()}} for the required schema.
}
\description{
Implementation of \code{\link[=get_assessments]{get_assessments()}} for the Greek \strong{ΗΠΜ / EPRM}
(Ηλεκτρονικό Περιβαλλοντικό Μητρώο — Electronic Environmental Registry),
the Ministry of Environment & Energy's (ΥΠΕΝ) public registry of
environmental-permit decisions, served by a public JSON:API at
\code{api.eprm.ypen.gr}.
}
\section{What this handler returns (IMPORTANT — decisions only)}{

The public registry exposes \strong{AEPO decisions only} — \emph{Αποφάσεις Έγκρισης
Περιβαλλοντικών Όρων} (decisions approving the environmental terms of a
project), i.e. the regulatory \strong{output} of the EIA (ΜΠΕ) process. The
underlying \strong{ΜΠΕ study files and all ΣΜΠΕ / SEA records are behind the
gov.gr login} on \code{platform.eprm.ypen.gr} and are \strong{not} reachable here.
So each record carries the decision metadata plus (at most) one decision
PDF — never the EIA study itself, and the SEA register is entirely
out of scope. Plan for a decisions-only corpus when using this handler.
}

\section{URL enumeration}{

The portal is a SPA backed by a public, no-auth \strong{JSON:API}. Listing is
\code{GET /v1/license-decisions}, paginated via JSON:API page parameters
(\code{page[number]}, 1-based; \code{page[size]}, default 100). Each list row is
\strong{already the full record} — there is no separate detail call needed
(though \code{GET /v1/license-decisions/{id}} exists). The walk follows
\code{meta.total} / \code{links.next} until the register is exhausted (≈19,800
records). Every request carries \code{Accept: application/json}.

Server-side JSON:API \code{filter[...]} parameters honoured here:
\itemize{
\item \code{query} -> \code{filter[text_search]} (free-text over project/decision fields).
\item \code{type} -> \code{filter[type]} (decision-type enum; see
\code{\link[=get_assessments_coverage]{get_assessments_coverage()}} / \code{eprm_gr_facets()}).
\item \code{date_range} -> \code{filter[issued_after]} / \code{filter[issued_before]} (matched
server-side on the decision issue date), re-checked client-side as a guard.
}
}

\section{Geometry}{

A record's \code{project_location} is an array of \verb{\{lat, lon\}} pairs. When
present, the first pair is written as a \strong{Point} geometry next to the
sidecar as \verb{<document_id>.geometry.geojson}, in the family FeatureCollection
layout with the GeoJSON-2008 \code{crs} member naming
\verb{urn:ogc:def:crs:EPSG::4326}. The coordinates are already geographic
\strong{WGS84 (EPSG:4326)} — \emph{not} the Greek Grid (EPSG:2100) — so no
reprojection happens. The sidecar carries \code{geometry_path} and
\code{geometry_crs} (\code{"EPSG:4326"}). Records with no \code{project_location} leave the
geometry columns \code{NA}.
}

\section{Attachments}{

At most one document per decision — the AEPO decision PDF — taken verbatim
from \code{record.diavgeia_doc_url} (hosted on Διαύγεια / Diavgeia, the Greek
government transparency portal, form
\verb{https://diavgeia.gov.gr/luminapi/api/decisions/\{ADA\}/document.pdf}; the ADA
contains Greek characters, left for \code{httr2} to percent-encode at request
time). It is emitted under the single slug \code{attachment_urls_aepo} /
\code{local_path_aepo}, with the deduplicated union at \code{attachment_urls} /
\code{local_path} (required by the schema). Records whose \code{diavgeia_doc_url} is
\code{null} yield zero attachments (still schema-valid).
}

\section{Filter coverage (v0.1)}{

\itemize{
\item \code{query} — server-side full-text (\code{filter[text_search]}).
\item \code{type} — server-side decision-type enum (\code{filter[type]}), e.g.
\code{"aepo_creation"}; see \code{eprm_gr_facets()}.
\item \code{date_range} — server-side window on the issue date
(\code{filter[issued_after]} / \code{filter[issued_before]}), re-checked client-side
against \code{date_decision}. \code{date_published} is the registry publication
timestamp.
}
}

\section{Performance}{

Enumeration is ≈200 paginated list calls (no per-record detail fetch). GR
requests are throttled to 5 requests per second by default; override via
\code{getOption("planscanR.gr_throttle_rate")} (requests/sec; falsy disables).
}

\examples{
\dontrun{
# Quick smoke test (AEPO decisions only)
get_assessments_gr(limit = 3, download = FALSE)

# Photovoltaic-themed slice (server-side full-text, Greek)
get_assessments_gr(query = "φωτοβολταϊκ", limit = 20, download = FALSE)

# New AEPO approvals only
get_assessments_gr(type = "aepo_creation", limit = 20, download = FALSE)
}
}
\seealso{
\code{\link[=get_assessments]{get_assessments()}}, \code{\link[=get_assessments_coverage]{get_assessments_coverage()}}.
}
