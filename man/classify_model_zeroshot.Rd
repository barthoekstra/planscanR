% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_zeroshot.R
\name{classify_model_zeroshot}
\alias{classify_model_zeroshot}
\title{Local zero-shot classifier backed by a HuggingFace NLI model.}
\usage{
classify_model_zeroshot(
  model_id = "MoritzLaurer/mDeBERTa-v3-base-xnli-multilingual-nli-2mil7",
  device = NULL
)
}
\arguments{
\item{model_id}{HuggingFace model ID.}

\item{device}{Torch device: \code{"mps"}, \code{"cuda"}, \code{"cpu"}, or \code{NULL} to
auto-detect (MPS on Apple Silicon, else CUDA, else CPU).}
}
\value{
A \code{planscanR_classifier_zeroshot} S3 object.
}
\description{
Lazily constructs a \code{transformers} \code{zero-shot-classification} pipeline on
first use (cached for the session). The default model is the multilingual
NLI model \verb{MoritzLaurer/mDeBERTa-v3-base-xnli-multilingual-nli-2mil7}, which
handles German / Dutch / English text natively — no translation required.
}
\details{
Requires the Python packages \code{transformers}, \code{torch}, \code{sentencepiece} and
\code{protobuf} (the last two are needed by the mDeBERTa tokenizer). Install with
\code{reticulate::py_require(c("transformers","torch","sentencepiece","protobuf"))}.
}
\examples{
\dontrun{
clf <- classify_model_zeroshot()
labels <- c(wind = "wind energy project", other = "unrelated to energy")
classify_text(clf, c("Windpark Test", "Wohnungsbau"), labels)
}
}
