% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_assessments_at.R
\name{get_assessments_at}
\alias{get_assessments_at}
\title{Fetch environmental-assessment records from Austria.}
\usage{
get_assessments_at(
  date_range = NULL,
  limit = Inf,
  download = TRUE,
  cache_dir = NULL,
  overwrite = FALSE,
  max_file_size_mb = NULL,
  write_sidecar = TRUE,
  refresh = FALSE,
  topic = NULL,
  relevance_threshold = NULL,
  relevance_model = NULL,
  query = NULL,
  jurisdiction = NULL,
  ...
)
}
\arguments{
\item{date_range}{Length-2 vector \code{c(from, to)} of dates or parseable
strings. Compared against the record's \code{year}; see \emph{Filter coverage}.}

\item{limit}{Integer. Maximum records to return. Defaults to \code{Inf}.
The full register is small (~500 records), so a cold-cache full
crawl completes in a few minutes.}

\item{download, cache_dir, overwrite, max_file_size_mb, write_sidecar, refresh}{See \code{\link[=get_assessments]{get_assessments()}}. \code{download}, \code{overwrite}, and
\code{max_file_size_mb} are accepted but ignored — no PDFs are reachable.}

\item{topic, relevance_threshold, relevance_model}{Forwarded from
\code{\link[=get_assessments]{get_assessments()}}. \code{relevance_threshold} is documented as a
download-gate; on AT it has no observable effect because there are
no downloads to gate.}

\item{query}{Free-text substring match on \code{title} + \code{summary} (client-
side). The portal has no server-side full-text search.}

\item{jurisdiction}{Character vector. Substring match against
\code{bundeslaender} (the comma-joined Austrian federal-state list).}

\item{...}{Reserved for future extensions; unused arguments are warned about.}
}
\value{
A tibble; see \code{\link[=get_assessments]{get_assessments()}} for the required schema.
}
\description{
Implementation of \code{\link[=get_assessments]{get_assessments()}} for Austria. Backed by the UVP-DB
run by Umweltbundesamt at \url{https://secure.umweltbundesamt.at/uvpdb/public}.
Compared to the NL and DE handlers, the AT portal is \strong{metadata-only}:
the procedure register and per-procedure summary are exposed via open
JSON service handlers, but every document attachment lives behind a
Keycloak login and is therefore \strong{not retrievable} by this version.
}
\section{URL enumeration}{

The public HTML pages of the portal sit behind a Keycloak login wall.
Three JSON service handlers, however, are open:

\if{html}{\out{<div class="sourceCode">}}\preformatted{https://secure.umweltbundesamt.at/uvpdb/?servicehandler=mapsdata
https://secure.umweltbundesamt.at/uvpdb/?servicehandler=mapsgeom
https://secure.umweltbundesamt.at/uvpdb/?servicehandler=vorhabenInfo&v2id=<id>
}\if{html}{\out{</div>}}

Enumeration is a single \code{mapsdata} call that returns ~500 records keyed
by Aktenzahl (AZ), each carrying \code{v2id}, \code{province}, \code{year}, \code{title},
and \code{type}. Per-record detail comes from one \code{vorhabenInfo} call per
\code{v2id}. There is no pagination, CSRF, or session requirement; the
typology mapping (\code{type} integer → German legend) is captured as a
static constant in this file because the portal rarely changes it.
}

\section{Filter coverage (v0.1)}{

\itemize{
\item \code{query} — case-insensitive substring match against \code{title} + \code{summary}.
\item \code{date_range} — matched against \code{year}, treating each record's year as
the full January 1 – December 31 window. \code{date_decision} is \strong{always
NA} because the portal does not expose a decision or last-modified
timestamp to anonymous callers; a synthetic mid-year date would
pretend to a precision the source lacks.
\item \code{jurisdiction} — substring match against \code{bundeslaender} (the comma-
joined Austrian federal-state list, e.g. \code{jurisdiction = "Bayern"}
never matches; \code{jurisdiction = "Wien"} keeps Viennese records).
}
}

\section{Attachments}{
 not available:
The Austrian portal does not expose document URLs to anonymous callers.
For every record this handler returns \code{attachment_urls = character(0)},
\code{local_path = character(0)}, and an empty \code{download_status} tibble.
The \code{download} argument is accepted for API symmetry but has no effect.
Authenticated access (UBA Keycloak) is out of scope for v0.1.
}

\examples{
\dontrun{
# Quick smoke test
get_assessments_at(limit = 3, download = FALSE)

# Windkraft-only slice
get_assessments_at(query = "Windpark", limit = 20, download = FALSE)

# All Burgenland records from a given year window
get_assessments_at(
  date_range = c("2016-01-01", "2018-12-31"),
  jurisdiction = "Burgenland",
  download = FALSE
)
}
}
\seealso{
\code{\link[=get_assessments]{get_assessments()}}, \code{\link[=get_assessments_coverage]{get_assessments_coverage()}}.
}
