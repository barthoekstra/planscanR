% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_public.R
\name{cache_path}
\alias{cache_path}
\title{Compute the local cache path an attachment URL would land at.}
\usage{
cache_path(url, country, document_id, cache_dir = cache_dir_default())
}
\arguments{
\item{url}{Source URL (character scalar).}

\item{country}{ISO-2 country code.}

\item{document_id}{Portal-native document ID.}

\item{cache_dir}{Cache root. Defaults to \code{\link[=cache_dir_default]{cache_dir_default()}}.}
}
\value{
Absolute path (character scalar). The parent directory is created
as a side effect.
}
\description{
Returns the deterministic destination path for an attachment without
downloading anything. The layout is
\verb{<cache_dir>/files/<country>/<document_id>/<slug>}, where the slug is a
flatten-safe, lowercase ASCII basename derived from the URL (see the package
download docs). Computing this up front lets a caller decide whether to
fetch a URL — e.g. checking whether the file already exists locally or
remotely — before spending a request on it.
}
\details{
For URLs whose path carries no file extension, the returned path uses a \code{.x}
placeholder extension; the real extension is only known after the body has
been fetched (see \code{\link[=resolve_cached_path]{resolve_cached_path()}} and \code{\link[=download_to_cache]{download_to_cache()}}).
}
\examples{
cache_path(
  "https://pas.commissiemer.nl/files/nl/3619/a3619ts.pdf",
  country = "nl",
  document_id = "3619",
  cache_dir = tempdir()
)
}
\seealso{
\code{\link[=resolve_cached_path]{resolve_cached_path()}}, \code{\link[=download_to_cache]{download_to_cache()}}
}
