% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_assessments.R
\name{get_assessments}
\alias{get_assessments}
\title{Retrieve environmental-assessment records from a national portal.}
\usage{
get_assessments(
  country,
  date_range = NULL,
  limit = Inf,
  download = TRUE,
  cache_dir = NULL,
  overwrite = FALSE,
  max_file_size_mb = NULL,
  write_sidecar = TRUE,
  refresh = FALSE,
  topic = NULL,
  relevance_threshold = NULL,
  relevance_model = NULL,
  ...
)
}
\arguments{
\item{country}{Character scalar, ISO-3166-1 alpha-2 country code (any case).
v0.1 supports \code{"nl"}, \code{"de"}, and \code{"at"}. See \code{\link[=supported_countries]{supported_countries()}}.}

\item{date_range}{Optional length-2 vector \code{c(from, to)} of \code{Date},
\code{POSIXct}, or character. Filters by \code{date_published} / \code{date_decision}
semantics decided per handler.}

\item{limit}{Maximum number of records to return (default \code{Inf}).}

\item{download}{Logical. If \code{TRUE} (default), attachment files referenced by
each record are downloaded into the local cache. If \code{FALSE}, only metadata
is returned; \code{local_path} is \code{character(0)} in every row.}

\item{cache_dir}{Optional cache root. Defaults to
\code{tools::R_user_dir("planscanR", "cache")}.}

\item{overwrite}{Logical. If \code{TRUE}, re-download attachments even when a
cached copy exists.}

\item{max_file_size_mb}{Numeric cap (in MiB) on per-file download size.
URLs whose announced or actual size exceeds the cap are skipped and
recorded in the \code{download_status} column with status \code{"skipped_size"}.
\code{NULL} (default) defers to \code{getOption("planscanR.max_file_size_mb", 50)};
\code{Inf} disables the cap.}

\item{write_sidecar}{Logical. If \code{TRUE} (default), every returned record is
persisted to a \verb{<document_id>.meta.json} file alongside its attachments
so the cache can be re-indexed offline via \code{\link[=index_cache]{index_cache()}}.}

\item{refresh}{Logical. If \code{FALSE} (default), records for URLs that already
have a sidecar JSON on disk are loaded directly from the sidecar — no
detail-page HTTP request at all. This makes re-scoring against new topics
essentially free once a slice has been cached. Set to \code{TRUE} to force
fresh detail-page fetches (useful if the portal genuinely changed).}

\item{topic}{Optional character vector of topic phrases. Pass a single
string (e.g. \code{"wind energy infrastructure"}) or a named vector like
\code{c(wind = "wind energy", solar = "solar energy", res = "regional energy transition strategy")}. Each candidate record's
title+summary is embedded \strong{once} per call and scored by cosine
similarity against every topic, \strong{before} any attachments are downloaded.
Adds one \verb{relevance_score_<slug>} column per topic plus a shared
\code{relevance_model}. Unnamed topics get auto-slugified from the phrase.
Adding extra topics costs essentially nothing — the per-record embed is
the expensive step and runs once. Every scored record is sidecar'd and
returned regardless of its score.}

\item{relevance_threshold}{Optional cutoff in \verb{[-1, 1]}. \strong{Download-gate
only}: records that score below the threshold still appear in the
returned tibble and still get a sidecar JSON on disk — only their PDF
attachments are skipped. This lets you re-run with a different threshold
(or no threshold) later without re-hitting the portal. Scalar threshold:
PDFs are downloaded if \strong{any} topic clears it. Named numeric vector
(e.g. \code{c(wind = 0.5, solar = 0.4)}): per-topic cutoffs, downloads happen
if \strong{any} named topic clears its own cutoff. \code{NULL} (default) is
score-only; every record's PDFs are downloaded (when \code{download = TRUE}).}

\item{relevance_model}{A \code{planscanR_embedding_model}. Defaults to
\code{\link[=embedding_model_minilm]{embedding_model_minilm()}} (sentence-transformers
\code{paraphrase-multilingual-MiniLM-L12-v2} via reticulate). Pass a custom
one built with \code{\link[=embedding_model]{embedding_model()}} to plug in a different backend.}

\item{...}{Forwarded to the country handler. Common search parameters are
documented in \code{\link[=get_assessments_nl]{get_assessments_nl()}}.}
}
\value{
A tibble with at least these required columns:
\describe{
\item{\code{country}}{Character (ISO-2, lowercase).}
\item{\code{source_portal}}{Character (e.g. \code{"commissiemer.nl"}).}
\item{\code{document_id}}{Character. Portal-native ID, unique within the portal.}
\item{\code{url}}{Character. Canonical portal landing URL.}
\item{\code{retrieved_at}}{POSIXct (UTC).}
\item{\code{attachment_urls}}{List-column of character vectors.}
\item{\code{local_path}}{List-column of character vectors (parallel to \code{attachment_urls}).}
}
Handlers may add further columns such as \code{title}, \code{native_type}, \code{status},
\code{date_published}, \code{competent_authority}, \code{proponent}, \code{jurisdiction}, etc.
No cross-portal normalisation is applied to these — values stay in the
source portal's vocabulary.
}
\description{
Unified entry point that dispatches on \code{country} to a per-country handler.
Returns a tibble of records. The shape of the returned tibble follows the
planscanR schema: a small required-columns set is guaranteed across
countries, and any additional columns exposed by the portal are appended
freely. See \strong{Return value} for details.
}
\details{
Per-country search parameters are forwarded through \code{...}. Some are
implemented by every handler that supports them (e.g. \code{theme},
\code{advice_type}, \code{province}, \code{status}, \code{query}); consult the per-country
handler's documentation (e.g. \code{\link[=get_assessments_nl]{get_assessments_nl()}}) for the exact
parameter surface and the vocabulary of valid values.
}
\seealso{
\code{\link[=get_assessments_coverage]{get_assessments_coverage()}}, \code{\link[=get_assessments_nl]{get_assessments_nl()}},
\code{\link[=supported_countries]{supported_countries()}}.
}
