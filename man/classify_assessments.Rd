% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_assessments.R
\name{classify_assessments}
\alias{classify_assessments}
\title{Classify assessment records with a zero-shot model.}
\usage{
classify_assessments(
  records,
  classifier = NULL,
  labels = biogain_classification_labels(),
  multi_label = FALSE,
  batch_size = 16L,
  write_sidecar = FALSE
)
}
\arguments{
\item{records}{A tibble (from \code{\link[=get_assessments]{get_assessments()}} / \code{\link[=index_cache]{index_cache()}}); must
have at least \code{title} (and ideally \code{summary}, \code{native_type}).}

\item{classifier}{A \code{planscanR_classifier}. Defaults to
\code{\link[=classify_model_zeroshot]{classify_model_zeroshot()}}.}

\item{labels}{Named character vector of candidate labels (see
\code{\link[=biogain_classification_labels]{biogain_classification_labels()}}, the default). A \code{relevant} attribute
marks which slugs are BIOGAIN-relevant; if absent, every label is treated
as relevant.}

\item{multi_label}{If \code{FALSE} (default) labels are mutually exclusive
(softmax); the negative classes then compete with the positive ones,
which is the point. \code{TRUE} scores each label independently.}

\item{batch_size}{Number of records per classifier call (progress + memory).}

\item{write_sidecar}{If \code{TRUE}, persist the verdict into each record's
sidecar JSON. Default \code{FALSE} (in-memory only).}
}
\value{
\code{records} with the \verb{class_*} columns added.
}
\description{
Offline pass (no portal calls): for each record, classifies
title + summary + category against \code{labels} and adds the verdict as new
columns. Pairs with \code{\link[=index_cache]{index_cache()}} and \code{\link[=score_assessments]{score_assessments()}} — same
harvest-broad-classify-later workflow.
}
\details{
Added columns:
\itemize{
\item \code{class_label} — best label slug.
\item \code{class_score} — probability of the best label.
\item \code{class_relevant} — \code{TRUE} if the best label is in the \code{relevant}
attribute of \code{labels} (i.e. an energy class, not a negative class).
\item \code{class_model} — the classifier's name.
\item \verb{class_score_<slug>} — one column per label with its probability.
}
}
\examples{
\dontrun{
recs <- index_cache(country = "de")
classified <- classify_assessments(recs, write_sidecar = TRUE)
table(classified$class_label, classified$class_relevant)
}
}
